interface:
  display_name: "G4f.dev"
  short_description: "Repo-specific patterns and workflows for g4f.dev"
  default_prompt: "Use the g4f.dev repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true